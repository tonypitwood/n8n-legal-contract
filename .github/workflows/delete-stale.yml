name: 🔥 Delete Stale File

on:
  repository_dispatch:
    types: [sync-delete-v2]
    
jobs:
  delete:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout repo
        uses: actions/checkout@v3

      - name: 🛠 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📦 Install dependencies
        run: npm install axios

      - name: 🚀 Run deletion script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FILE_SHA: ${{ secrets.FILE_SHA }}
        run: node scripts/delete-stale.js